---
description: Mot de passe SSH VPS LibAutoEnt — ne pas redemander
alwaysApply: true
---

# Déploiement VPS LibAutoEnt

- Host: `51.255.162.99`, user: `ubuntu`, site: `https://libautoent.a2spr.com`
- Credentials: fichier local **gitignoré** `.env.deploy` (`VPS_SSH_HOST`, `VPS_SSH_USER`, `VPS_SSH_PASSWORD`) ou `.vps_ssh_password`.
- Script: `_tmp_vps_deploy_libautoent.py` — charge `.env.deploy` automatiquement.
- **Ne jamais** committer `.env.deploy` / `.vps_ssh_password`, ni coller le mot de passe dans git.
- Si les fichiers manquent, demander une seule fois et les réécrire.
- Ne pas redemander le mot de passe à chaque déploiement tant que `.env.deploy` existe.
